trigger ZeroBounceAPI on Account (after update) {
    
    /*Account acc = new Account();
    if(trigger.isupdate){
    for (Account acc : Trigger.new) {
        if(acc.ZeroBounce_API__c == true && acc.ZeroBounce_API__c != trigger.oldMap.get(acc.Id).ZeroBounce_API__c){
            FutureClass2.ZeroBounce(acc.Id);
        	}
        }
    }
    
    else if (trigger.isInsert){    
    for (Account acc : Trigger.new) {
        if(acc.On_Demand_Email_Check__c == true){
            ZbAPI_Futuremethod.ZeroBounce1(acc.Id);
        	}
    	}
    }*/
}